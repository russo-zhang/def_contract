// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract Block {
    //  block.number → 当前区块号
    //  block.timestamp → 区块时间戳（秒）
    //  block.gaslimit → 当前区块最大 gas
    //  block.chainid → 区块链 ID（最常用）
    //  block.coinbase → 区块矿工地址
    //  block.difficulty → 区块难度（很少用）
}
